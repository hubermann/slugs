<%= form_for(@fav) do |f| %>
  <% if @fav.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fav.errors.count, "error") %> prohibited this fav from being saved:</h2>

      <ul>
      <% @fav.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :url %><br />
   
    <%= f.text_field :url%>
    <p>write entirely url like http://www.domain.extencion</p>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
